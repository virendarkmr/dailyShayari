package com.dailyshayari.ui.theme

import androidx.compose.ui.graphics.Color

// Main App Colors
val GoldPrimary = Color(0xFFD4AF37)
val GoldSoft = Color(0xFFE0C97F)

// Background and Surface Colors
val BackgroundPrimary = Color(0xFF121212)
val SurfacePrimary = Color(0xFF1E1E1E)

// Text Colors
val TextPrimary = Color.White
val TextSecondary = Color(0xFFBDBDBD)

// The Gold color I added earlier, which is the same as GoldPrimary.
// We can keep it for now or remove it, but it's not causing issues.
val Gold = Color(0xFFD4AF37)
